#import core

#----- get the list and assci > create a mapdict and a reverce_dict 
#----- this is how idiot encode and decode passwords 
#----- disclaimer : the first line is not gonna work if password does't match the hash

#list, assci = core.IDIOT_Hashing.encode_apassword('?password','?hash')
#mapdict , reverce_dicr = core.IDIOT_Hashing.load_hash_table(assci, list)
#encoded_password = core.IDIOT_Hashing.encode(mapdict, '?password_you_want_to_encode')
#decoded_password = core.IDIOT_Hashing.decode(reverce_dicr, encoded_password)
#print(f" encoded \n {encoded_password}\n decoded \n {decoded_password}")







